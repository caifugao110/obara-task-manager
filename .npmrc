# better-sqlite3 v13 已在包内 prebuilds/ 目录自带各平台预编译二进制
# （基于 N-API，与 Node 具体版本无关，支持 Node 22+，含当前的 Node 24），
# 本来完全不需要 node-gyp 编译。但 npm 检测到包内 binding.gyp 后仍会隐式
# 执行 `node-gyp rebuild`；该包的 binding.gyp 在预编译存在时虽会把编译目标
# 置空，可 node-gyp 的 configure 阶段在 Windows 上仍强制查找 Visual Studio，
# 导致未安装 VS C++ 生成工具的机器上安装必然失败。
# 本仓库后端依赖均为纯 JS 或自带二进制；前端 vite/esbuild/rollup 的平台
# 二进制由 optionalDependencies 提供，均不依赖安装脚本。故全局跳过
# install 生命周期脚本，控制台(control)与 start.bat 的 npm install 都会
# 自动读取本文件（npm 会从当前目录逐级向上查找 .npmrc）。
ignore-scripts=true
